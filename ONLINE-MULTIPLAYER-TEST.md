# اختبار شامل للعب الجماعي

## الهدف

يغطي هذا الاختبار دورة الغرفة كاملة، مزامنة اللاعبين والجولات، الانتقال اليدوي والاحتياطي، إعادة الاتصال، الاعتراضات، وحالات السباق بين المضيف والضيف. يُفضّل تشغيله على متصفحين مستقلين أو جهازين مختلفين.

## المتطلبات

شغّل اللعبة من نسخة منشورة أو من خادم محلي. استخدم نافذة A للمضيف ونافذة B للضيف. أنشئ غرفة من **3 جولات** ومدة **30 ثانية** لتقليل وقت الاختبار. دوّن رمز الغرفة ووقت بدء كل جولة.

## مصفوفة الاختبارات

| المعرّف | السيناريو | الإجراء | النتيجة المتوقعة |
|---|---|---|---|
| M01 | إنشاء الغرفة | المضيف يختار لعب أونلاين ثم إنشاء غرفة | يظهر رمز غرفة من 5 أحرف، ويظهر المضيف في غرفة الانتظار |
| M02 | تحميل الحروف | اضغط ابدأ بعد مسح التخزين المحلي | تبدأ الجولة بحرف موجود في manifest، وليس `null` |
| M03 | انضمام الضيف | الضيف يدخل الاسم ورمز الغرفة | يظهر اللاعبان في غرفة الانتظار لدى الطرفين |
| M04 | حد الغرفة | أدخل 6 لاعبين ثم حاول إدخال لاعب سابع | تظهر رسالة أن الغرفة ممتلئة، ولا يُضاف اللاعب السابع |
| M05 | بدء الجولة | المضيف يضغط ابدأ مرتين بسرعة | تُنشأ جولة واحدة فقط، وتظهر نفس الجولة والحرف عند الطرفين |
| M06 | مزامنة الإدخال | اكتب في حقول الإجابات لدى الطرفين | يظهر نشاط الكتابة/حالة اللاعب بشكل متزامن دون تغيير إجابات الطرف الآخر |
| M07 | الإرسال المكرر | اضغط انتهيت مرتين بسرعة | يُرسل كل لاعب مرة واحدة، ويتعطل الزر، ولا تظهر أخطاء duplicate |
| M08 | انتهاء الوقت | لا تضغط انتهيت وانتظر 30 ثانية | تنتقل الجولة إلى شاشة المراجعة عند الطرفين |
| M09 | شاشة المراجعة | راقب الطرفين لمدة 10 ثوانٍ | يبقى الطرفان في المراجعة، ويظهر زر `الجولة التالية` للمضيف فقط |
| M10 | الانتقال اليدوي | المضيف يضغط `الجولة التالية` | ينتقل المضيف والضيف إلى الجولة 2 خلال 2–3 ثوانٍ، وبحرف جديد صالح |
| M11 | انتقال الضيف بالـpolling | عطّل/تجاهل Realtime إن أمكن ثم اضغط الزر من المضيف | ينتقل الضيف رغم غياب حدث Realtime بفضل polling كل 1.5 ثانية |
| M12 | سباق إنشاء الجولة | اضغط زر الجولة التالية بسرعة من الطرفين أو اترك شبكة الأمان تعمل | لا تظهر رسالة duplicate key، وتبقى جولة واحدة فقط في قاعدة البيانات |
| M13 | شبكة الأمان | في الجولة 2 لا تضغط زر المضيف | ينتقل الطرفان تلقائيًا بعد 60 ثانية تقريبًا |
| M14 | اعتراض مالك الإجابة | اكتب إجابة غير مدرجة باسم الضيف | لا يظهر زر 🚩 لصاحب الإجابة نفسه |
| M15 | رفع اعتراض | من لاعب آخر اضغط 🚩 على إجابة `غير مدرجة` | يُحفظ اعتراض واحد، ويظهر popup للمضيف فقط |
| M16 | قرار القبول | المضيف يضغط `قبول (+10)` | تزيد نقاط صاحب الإجابة 10، ويُسجّل القرار `accepted` |
| M17 | قرار الرفض | ارفع اعتراضًا آخر واستخدم `رفض` | يُسجّل القرار `rejected` دون تغيير النقاط |
| M18 | اعتراض مكرر | اضغط 🚩 على نفس الكلمة والفئة مرة أخرى | لا يُنشأ صف مكرر ولا يظهر خطأ مزعج |
| M19 | تحديث النتائج | أكمل الجولة 2 ثم الجولة 3 | تتطابق النقاط والنتائج النهائية عند الطرفين |
| M20 | انقطاع المضيف | أغلق متصفح المضيف أثناء مراجعة الجولة 2 | يستطيع الضيف الانتقال تلقائيًا بعد شبكة الأمان، ولا يتجمد |
| M21 | تحديث الضيف | نفّذ refresh للضيف أثناء اللعب أو المراجعة | يظهر استئناف الغرفة، ويعود الضيف للجولة الصحيحة |
| M22 | انتهاء اللعبة | أنهِ الجولة 3 | ينتقل الطرفان إلى `onlineResults` دون زر جولة جديدة |

## تحقق قاعدة البيانات

نفّذ الاستعلامات التالية من SQL Editor بعد الاختبار. الاستعلامات محدودة لتجنب قراءة غير محدودة:

```sql
select code, status, current_round, total_rounds, round_duration
from rooms
order by created_at desc
limit 5;
```

```sql
select room_id, number, letter, status
from rounds
where room_id = '<ROOM_UUID>'
order by number
limit 10;
```

يجب أن يحتوي الاختبار على جولة واحدة فقط لكل رقم. للتحقق من عدم وجود تكرارات:

```sql
select room_id, number, count(*) as copies
from rounds
group by room_id, number
having count(*) > 1
limit 20;
```

تحقق من الاعتراضات:

```sql
select word, category, host_decision, raised_at
from pending_words
where room_id = '<ROOM_UUID>'
order by raised_at
limit 20;
```

## معايير النجاح

يُعد الاختبار ناجحًا إذا تحققت الشروط التالية:

1. لا توجد جولة مكررة لأي `(room_id, number)`.
2. لا يظهر `null` في حقل `rounds.letter`.
3. ينتقل الضيف خلال 2–3 ثوانٍ بعد انتقال المضيف، حتى مع فقدان Realtime.
4. لا يختفي زر الاعتراض قبل قرار المضيف.
5. لا يستطيع صاحب الإجابة الاعتراض على كلمته.
6. كل اعتراض له قرار واحد فقط، وتُحدّث النقاط عند القبول فقط.
7. تعمل شبكة الأمان بعد 60 ثانية عند غياب المضيف.
8. تنتهي الجولة الأخيرة عند شاشة النتائج لدى الطرفين.

## تسجيل النتائج

| المعرّف | النتيجة | الملاحظات |
|---|---|---|
| M01–M04 | ☐ ناجح ☐ فشل | |
| M05–M08 | ☐ ناجح ☐ فشل | |
| M09–M13 | ☐ ناجح ☐ فشل | |
| M14–M18 | ☐ ناجح ☐ فشل | |
| M19–M22 | ☐ ناجح ☐ فشل | |

## فحص آلي سريع

من جذر المستودع:

```bash
bash tests/online-preflight.sh
```

هذا الفحص يتحقق من الصياغة، نظافة التغييرات، وجود polling، تحميل manifest، معالجة سباق إنشاء الجولة، مسار الاعتراضات، وزر الانتقال اليدوي وشبكة الأمان. لا يحل محل اختبار متصفحين حقيقيين.
